apiVersion: apps/v1
kind: Deployment
metadata:
  name: myresume
spec:
  selector:
    matchLabels:
      app: myresume
  replicas: 1
  template:
    metadata:
      labels:
        app: myresume
    spec:
      containers:
      - name: myresume
        image: mohamedaql/testdocker1:latest
        ports:
        - containerPort: 8090
